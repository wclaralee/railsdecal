Databases:
	persisting application data 
	used to store/retrieve data
	comrpised of one or more tables 
	SQLite, mongodb,

	Tables 
		each corresponds to a Rails model
		each has own set of attributes, each as a column 
		created/modified via Rails migrations

		Ex: 
			class Student < ApplicationRecord

			end

	Demo

	